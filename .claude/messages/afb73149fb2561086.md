# Message for Agent afb73149fb2561086

**From:** sad-fermat-cdf4ff  
**Date:** 2026-04-23  
**Priority:** CRITICAL UPDATE

---

## Use Real Unsplash Images for ALL Products

This is NOT a new task — just an update to use real data from existing files.

### Files to import from

Both files now exist at:

- `/Users/tarunchintakunta/Personal/Buildmart/frontend/src/constants/images.ts`
- `/Users/tarunchintakunta/Personal/Buildmart/frontend/src/constants/mockData.ts`

### What to do

1. **Use `MOCK_PRODUCTS` from `mockData.ts`** for all product listings. Each product already has an `.image` field containing a real Unsplash URL.

2. **Import pattern:**
   ```ts
   import { MOCK_PRODUCTS } from '../../src/constants/mockData';
   import { getProductImage } from '../../src/constants/images';
   ```

3. **For the `shop.tsx` product grid** — use `MOCK_PRODUCTS` from `mockData.ts`. Display images like this:
   ```tsx
   <Image source={{ uri: product.image }} />
   ```

4. **No blank images** — every product MUST show an image. Use the `.image` field from `MOCK_PRODUCTS`, or call `getProductImage()` from `images.ts` as a fallback.

5. **Supabase is now installed.** The frontend has `@supabase/supabase-js` installed. You can import it from:
   ```ts
   import { supabase } from '../../src/config/supabase';
   ```

### Summary

- Replace any placeholder/empty images with real Unsplash URLs from `MOCK_PRODUCTS`
- Apply this to **all screens** you are building that show products
- Use `<Image source={{ uri: product.image }} />` pattern throughout
