import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('BuildMart app smoke test', (WidgetTester tester) async {
    // Placeholder smoke test – full widget tests require Supabase mock setup
    expect(true, isTrue);
  });
}
